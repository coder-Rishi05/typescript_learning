it is intalled 2 ways

1. global
2. project based

to install 
    npm i -D typescript

to use

npx tsc --init

tsc is typescript compiler

it create a by default tsconfig.json file

npx tsc 

it basically require dist folder and then include default scripts to run typescript code


### Type annotation and interference

- type inferecing means

    jb mai koi code likhta hun to typescriopt apne aap uska type samaj jaata hai mughe btane ki jrurat nhi pdti.

    ![alt text](image.png)

- type annotation
    :number, :string :boolean
    let drink:string = "water"

types of annotation